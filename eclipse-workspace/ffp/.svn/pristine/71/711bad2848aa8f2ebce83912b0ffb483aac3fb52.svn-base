(function(window, undefined){
	/**
	 * 定义配置对象
	 */
	var config = {
		/**
		 * 应用国际化JS文件
		 */
		appLocale : '../../app/js/locale/app-'+server_consts.locale+server_consts.min,
		/**
		 * 应用上传实现		 */
		appUploader : '../../app/js/multiuploader-app'+server_consts.min
	};
	
	// 导出为全局变量
	window.beneform4jConfig = config;
})(this);