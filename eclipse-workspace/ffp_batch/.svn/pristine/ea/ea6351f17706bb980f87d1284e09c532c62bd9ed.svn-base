SET SCHEMA RTCXU01;
CREATE TABLE M_OVERSEAS_RECV_TEMP
	(
	OREF_NO           VARCHAR (16) NOT NULL,
	HOST_SEQ          VARCHAR (12),
	FIN_MSG_NO        VARCHAR (16),
	SND_BANK_ID       VARCHAR (16) NOT NULL,
	RLT_OREF_NO       VARCHAR (16),
	SND_BKNO          VARCHAR (11),
	SND_BKNM          VARCHAR (150),
	REV_BKNO          VARCHAR (11),
	REV_BKNM          VARCHAR (150),
	MSG_TYPE          VARCHAR (5),
	SESSION           VARCHAR (4),
	OSN               VARCHAR (6),
	ISN               VARCHAR (6),
	MSG_108T          VARCHAR (16),
	MSG_HEADER        VARCHAR (1),
	MSG_FORMAT        VARCHAR (4),
	ORG_MSG_DATE      VARCHAR (6),
	SUB_OSN           VARCHAR (4),
	SUB_ISN           VARCHAR (6),
	MSG_DATE          VARCHAR (10),
	MSG_TIME          VARCHAR (8),
	REV_MSG_DATE      DATE,
	REV_MSG_TIME      TIME,
	CONTENT           VARCHAR (216),
	DETAIL            VARCHAR (720),
	MSG_DETAIL        VARCHAR (1785),
	SUB_COPY          VARCHAR (2000),
	NETTLER           VARCHAR (20),
	NETTXDATE         DATE,
	NETTXTIME         TIME,
	NET_CHK_TXTLER    VARCHAR (10),
	NET_CHK_TXDATE    DATE,
	NET_CHK_TXTIME    TIME,
	TXTLER            VARCHAR (20),
	TXDATE            DATE,
	TXTIME            TIME,
	CHK_TXTLER        VARCHAR (20),
	CHK_TXDATE        DATE,
	CHK_TXTIME        TIME,
	CURR_NETNO        VARCHAR (7),
	TRAN_NETNO        VARCHAR (7),
	MATCH_FLAG        VARCHAR (1) NOT NULL DEFAULT '0',
	WITHIN_DEAL_FLAG  VARCHAR (2) NOT NULL DEFAULT '00',
	DEAL_STATUS       VARCHAR (1) NOT NULL DEFAULT '0',
	DEAL_FLAG         VARCHAR (1) NOT NULL DEFAULT '0',
	IN_OUT_FLAG       VARCHAR (1) NOT NULL DEFAULT '0',
	TEMPLET_NAME      VARCHAR (40) DEFAULT '-1',
	NEW_PAY_NO        VARCHAR (16),
	REVERT_MEMO       VARCHAR (300),
	APV_MEMO          VARCHAR (200),
	REVERT_TIME_LIMIT INTEGER NOT NULL DEFAULT -1,
	SUMMARY_LEVEL     VARCHAR (1) NOT NULL DEFAULT '0',
	SUMMARY_NO        VARCHAR (5),
	DIRE_CONTENT      VARCHAR (500),
	BACK_BANK_ID      VARCHAR (8),
	MEMO              VARCHAR (250),
	PROC_INS_ID       VARCHAR (20),
	RLT_TAB_NAME      VARCHAR (20),
	RLT_MATCH_FIELD   VARCHAR (20),
	BACK_DATE         VARCHAR (10),
	BACK_CODE         VARCHAR (4),
	BACK_DESC         VARCHAR (80),
	DATA_FLAG         VARCHAR (1),
	PRIMARY KEY (OREF_NO)
	)
	IN MSGSPACE
	INDEX IN MSGINDEXSPACE;

ALTER TABLE M_OVERSEAS_RECV_TEMP DATA CAPTURE NONE;
ALTER TABLE M_OVERSEAS_RECV_TEMP NOT VOLATILE;
ALTER TABLE M_OVERSEAS_RECV_TEMP APPEND OFF;
ALTER TABLE M_OVERSEAS_RECV_TEMP LOCKSIZE ROW;


